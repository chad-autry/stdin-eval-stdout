---
sudo: required
services:
  - docker

script:
- docker build -t chadautry/stdin-eval-stdout:latest .

after_script:
- docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
- docker push chadautry/stdin-eval-stdout:latest